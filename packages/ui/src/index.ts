/**
 * @easydeck/ui — the web configurator (milestone 3).
 *
 * Placeholder. Will become a browser SPA talking to the daemon's WebSocket
 * API; can later be wrapped in Tauri/Electron for a native feel.
 */
export {};
