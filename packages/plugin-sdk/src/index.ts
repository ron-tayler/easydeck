/**
 * @easydeck/plugin-sdk — plugin authoring kit (post-MVP).
 *
 * Plugins will run as separate processes speaking the daemon's WebSocket
 * protocol, so they can be written in any language and cannot crash the
 * daemon. This package will hold the TypeScript types and a client helper.
 */
export {};
