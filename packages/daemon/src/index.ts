/**
 * @easydeck/daemon — the composition root (milestone 3).
 *
 * Planned: connects Surfaces from @easydeck/device to the engine, serves the
 * web configurator over HTTP and a JSON WebSocket API, stores profiles as
 * JSON files in the user directory, runs built-in actions.
 */
export {};
