/**
 * @easydeck/engine — the logic zone (milestone 2).
 *
 * Planned model: Profile -> Page -> Button -> State, actions bound to key
 * events (down / up / longPress), and a reactive VariableStore that marks
 * buttons dirty so the renderer can repaint them.
 */
export {};
