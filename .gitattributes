# Keep line endings LF in the repository regardless of the platform a
# contributor works on — this project targets Windows, Linux and macOS.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ttf binary
